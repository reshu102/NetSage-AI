export function ruleChecker({symptom='',topologyNotes='',showOutputs=''}){const text=`${symptom}\n${topologyNotes}\n${showOutputs}`.toLowerCase(), out=[];const warn=(label,detail)=>out.push({label,status:'warning',detail}), ok=(label,detail)=>out.push({label,status:'success',detail});
 if(/administratively down|\bdown\b/.test(showOutputs.toLowerCase()))warn('Interface status warning','An interface appears down or administratively down in the supplied output.');else ok('Interface status checked','No down interface was detected in the supplied output.');
 const vlans=[...text.matchAll(/vlan\s*(\d+)/gi)].map(x=>x[1]);if(vlans.length&&/show vlan/i.test(showOutputs)&&!new RegExp(`\\b${vlans[0]}\\b`).test(showOutputs.replace(/vlan\s*\d+/i,'')))warn('VLAN may be missing',`VLAN ${vlans[0]} is referenced but was not confirmed in show vlan output.`);else ok('VLAN configuration checked','No obvious VLAN mismatch was found.');
 if(/unreach|cannot reach|different vlan|another network/.test(text)&&/show ip route/i.test(showOutputs))warn('Possible missing route','Cross-network connectivity is reported; confirm the destination network exists in the route table.');else ok('Route check completed','No deterministic missing-route signature found.');
 if(/gateway|default gateway/.test(text))warn('Gateway verification needed','Validate client gateway, SVI/subinterface address, and subnet mask alignment.');return out;}
