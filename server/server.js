import 'dotenv/config';import express from 'express';import cors from 'cors';import mongoose from 'mongoose';import routes from './routes/cases.js';
const app=express();app.use(cors());app.use(express.json({limit:'1mb'}));app.use('/api',routes);app.use((e,req,res,next)=>{console.error(e);res.status(500).json({message:'Server error. Check MongoDB and try again.'});});const port=process.env.PORT||5000;mongoose.connect(process.env.MONGO_URI||'mongodb://127.0.0.1:27017/netsage').then(()=>app.listen(port,()=>console.log(`NetSage API listening on ${port}`))).catch(e=>{console.error('MongoDB connection failed:',e.message);process.exit(1)});
